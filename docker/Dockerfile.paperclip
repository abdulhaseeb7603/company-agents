FROM node:20-slim
RUN apt-get update && apt-get install -y --no-install-recommends curl && \
    rm -rf /var/lib/apt/lists/*
RUN npm install -g paperclip hermes-paperclip-adapter
VOLUME /paperclip
EXPOSE 3100
ENTRYPOINT ["paperclip"]
